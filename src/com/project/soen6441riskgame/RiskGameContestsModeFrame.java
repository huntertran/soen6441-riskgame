package com.project.soen6441riskgame;

import javax.swing.JFrame;

public class RiskGameContestsModeFrame extends JFrame {
	
	
	/**
	 * this Class should be for creating the frame for the contests mode. 
	 * 
	 * i think " This frame should contain two Jlists 
	 * i.e Available maps and Available players which gets populated on frame load. 
	 *  
	 *  where User can select the maps and players from the available lists and move them to the selected lists using "Move" button.
	 * 
	 *  and where  User can select number of games from the dropdown and enter the maximum number of turns in the textfield. 
	 * 
	 * 
	 *
	 */

}
